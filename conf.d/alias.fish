# grep colors
alias grep='grep --color=auto'

# ls colors
alias ls="lsd"

# yeet
alias yeet="yay -Rns"
alias yeti="yay -Rnsc"

# lazy commands
alias l='ls -l'
alias la='ls -a'
alias lla='ls -la'
alias lt='ls --tree'

# lazy apps
alias n='nvim'
alias gg='lazygit'
alias nrd='npm run dev'
alias r='ranger'

alias cP='rsync --progress'
