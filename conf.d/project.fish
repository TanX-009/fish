# start andy android
alias andy="bash -c 'source /etc/profile && QT_QPA_PLATFORM=xcb emulator @andy -gpu host'"
alias amdy="bash -c 'source /etc/profile && QT_QPA_PLATFORM=xcb emulator @amdy -gpu host'"
